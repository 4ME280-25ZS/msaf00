// Copy this file to `wishlist/supabase.js` and replace the values with your project credentials.
// Keep this file out of source control if you add real keys.

// Copy this file to `wishlist/supabase.js` and replace the values with your project credentials.
// Keep the real `wishlist/supabase.js` out of source control.

// Example placeholder config (do NOT commit real keys):
window.SUPABASE_CONFIG = {
  SUPABASE_URL: 'https://<your-project-ref>.supabase.co',
  SUPABASE_ANON_KEY: '<your-anon-key>'
};

// If you don't plan to use Supabase, you can ignore this file entirely —
// the wishlist page falls back to localStorage when no config is present.
